.user-wrapper
  .edit-form
    %h1
      アカウント情報の編集
    = form_for (current_user),class:"form" do |f|
      .edit-form__image
        - if @user.image.present?
          .current-image
            = image_tag @user.image.url
        - else
          .no-image
            no-image
        .image-label-wrapper
          %label{ class: 'image_input_btn'}
            画像を選択
            =icon('fas', 'image')
            =f.file_field :image , class: "image_form"
          %p
            ※正方形に調整されます※

      .edit-form__name
        =f.label :名前 ,class:'label'
        =f.text_field :name ,class:'name-form'
        
      .edit-form__bio
        =f.label :自己紹介 ,class:'label'
        =f.text_field :bio ,class:'bio-form'

      .edit-form__email
        =f.label :メールアドレス ,class:'label'
        =f.email_field :email ,class:'email-form'

      .edit-btn
        =f.submit "更新する" ,class:'update-btn'

    .pass
      = link_to edit_user_registration_path do
        パスワードの変更 / アカウント削除


